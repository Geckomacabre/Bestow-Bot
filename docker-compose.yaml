services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    ports:
      - "3000:3000"
    command: ['bun', 'run', 'dev']
